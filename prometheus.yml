global:
  scrape_interval: 15s        # har 15 second mein data uthao

scrape_configs:
  - job_name: "node_exporter"
    static_configs:
      - targets: ["localhost:9100"]   # yahan se data lena hai
